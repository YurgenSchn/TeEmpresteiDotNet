namespace EstudoDividas.Contracts
{
    public class RegisterUserResponseContract : MinimalResponseContract { }
    // Identico ao minimal response.
    // Só criei esta classe para manter o padrão, request + response.
}
